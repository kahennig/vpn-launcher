# Checklist — T012: CLI --add y --status

- [x] `--add` pide alias, version, config, auth_mode interactivamente
- [x] `--add` valida alias único
- [x] `--add` appende a connections.conf
- [x] `--status` muestra si openvpn está corriendo con PID
- [x] `-s` como alias de `--status`
- [x] Usage actualizado con los nuevos comandos
